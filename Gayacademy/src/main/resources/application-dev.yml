spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gayacademy
    username: gaya
    password: gaya_dev_2025
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: false
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  data:
    redis:
      host: localhost
      port: 6379
      password: redis_dev_2025
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
          min-idle: 2
    mongodb:
      uri: mongodb://gaya:mongo_dev_2025@localhost:27017/gayacademy_chat?authSource=admin

  rabbitmq:
    host: localhost
    port: 5672
    username: gaya
    password: rabbit_dev_2025
    listener:
      simple:
        acknowledge-mode: manual

app:
  security:
    jwt:
      secret: ${JWT_SECRET:troque-esta-chave-em-producao-min-256-bits-aleatorios-gaya-academy-dev}
      access-token-expiration-minutes: 15
      refresh-token-expiration-days: 30
      issuer: gayacademy-api
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:8081
  storage:
    s3:
      endpoint: http://localhost:9000
      region: us-east-1
      bucket: gaya-media
      access-key: gaya_minio_admin
      secret-key: minio_dev_2025
      public-base-url: http://localhost:9000/gaya-media
      presigned-url-expiration-seconds: 180

logging:
  level:
    root: INFO
    com.gayacademy: DEBUG
    org.springframework.security: INFO